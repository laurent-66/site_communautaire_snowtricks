{% extends "base.html.twig" %}
{% block title %}Gérer profil{% endblock %}

{% block body %} 
<div class="container-fluid mx-0 my-0 px-0 mx-0" id="updateProfil">
    <div class="container-fluid container-body">
        <div class="row">
            <div class="col-sm-12 col-md-8 col-lg-8 mx-auto">
                <div class="container card rounded shadow p-3 mb-5">
                    {% if error %}
                    <div class="alert alert-danger" role="alert">
                        {{ error }}
                    </div>
                    {% endif %}
                    <h2 class="text-center">Mon profil</h2>
                        <div class="col-sm-12 col-md-12 col-lg-12 mx-auto profile-picture">
                            <img src="https://placehold.it/64x64" alt="photo laurent" class="rounded-circle">
                        </div>
                    <div class="col-sm-12 col-md-12 col-lg-12 mx-auto">
                        {{form_start(form)}}
                        {{form_widget(form)}}
                        <button class="btn btn-lg btn-primary btn-block mb-3" type="submit">Modifier</button>
                        <p>Vous souhaitez supprimer votre profil ? Veuillez cliquer sur supprimer. Attention cette opération est irréversible ! </p>
                        <button class="btn btn-lg btn-danger btn-block mb-3" type="submit">Supprimer</button>   
                        {{form_end(form)}}
                    </div>
                </div>
            </div>
        </div>       
    </div>
</div>
{% endblock %} 